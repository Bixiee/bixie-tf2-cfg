
// Viewmodel FOV stuff
alias vFovDEF "viewmodel_fov 54"
alias vFov70 "viewmodel_fov 70"
alias vFov80 "viewmodel_fov 80"
alias vFov90 "viewmodel_fov 90"
alias vfov110 "viewmodel_fov 100"

// Viewmodel minmode stuff
alias vMin0 "tf_use_min_viewmodels 0"
alias vMin1 "tf_use_min_viewmodels 1"

// Viewmodel draw stuff
alias vDraw0 "r_drawviewmodel 0"
alias vDraw1 "r_drawviewmodel 1"

//binds to alias. 
bind 1 "aSlot1"
bind 2 "aSlot2"
bind 3 "aSlot3"
bind 4 "aSlot4"
bind 5 "aSlot5"
bind 6 "aSlot6"

// slots 5 and 6 are just there bc of the engineer config,
// it do nothing on other classes, as they are bound to null.
// Also, aSlot4 is only used for the spy config

//? the viewmodels on this cfg work by simply re-aliasing the "aSlot(n)" 
//? commands. Feel free to check the class cfgs (in the ClassCFGs folder)